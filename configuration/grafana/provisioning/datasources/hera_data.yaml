apiVersion: 1
datasources:
  - name: hera_data
    type: postgres
    editable: true
    access: proxy
    url: postgresql-container:5432
    database: hera_data
    user: postgres
    secureJsonData:
      password: 'postgres'
    jsonData:
      sslmode: 'disable'
